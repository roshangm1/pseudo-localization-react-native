import PseudoLocalizationProvider from './PseudoLocalizationContext';
import pseudoLocalizeString from './pseudoLocalizeString';

export { PseudoLocalizationProvider, pseudoLocalizeString };

const a = () => {
  return 'b';
};
export default a;
